---
paths:
  - "**/src/**/*.tsx"
---

# React コンポーネント規約

- React コンポーネントには必ず `displayName` を設定する
  - 理由: React DevTools やエラー境界・スタックトレースでコンポーネント名が表示され、デバッグやレビューが容易になる。匿名関数や `forwardRef` / `memo` でラップしたコンポーネントは表示名が欠落しやすいため明示する
