language js

// `as` 型アサーションは型システムを欺き、実体と乖離した型を握りつぶしてランタイムエラーの
// 温床になるため原則禁止する。型を狭めるだけで安全な `as const` は許可する。
TsAsExpression(ty=$type) where {
  $type <: not r"^const$",
  register_diagnostic(span=$type, message="`as` 型アサーションは原則禁止です。型ガードや適切な型定義で対応してください（やむを得ない場合は理由付きで biome-ignore してください）。")
}
