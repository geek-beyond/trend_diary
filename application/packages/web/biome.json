{
  "$schema": "https://biomejs.dev/schemas/2.4.16/schema.json",
  "root": false,
  "extends": ["../../biome.json"],
  "files": {
    "includes": ["**", "!src/client/components/shadcn", "!src/client/components/customized"]
  },
  "plugins": ["./biome-plugins/no-use-direct-dom-a-element.grit"],
  "css": {
    "parser": {
      "tailwindDirectives": true
    }
  },
  "html": {
    "formatter": {
      "selfCloseVoidElements": "always"
    }
  },
  "linter": {
    "rules": {
      "a11y": {
        "noAccessKey": "error",
        "noAriaUnsupportedElements": "error",
        "noAutofocus": "error",
        "noDistractingElements": "error",
        "noHeaderScope": "error",
        "noInteractiveElementToNoninteractiveRole": "error",
        "noLabelWithoutControl": "error",
        "noNoninteractiveElementToInteractiveRole": "error",
        "noNoninteractiveTabindex": "error",
        "noPositiveTabindex": "error",
        "noRedundantAlt": "error",
        "noRedundantRoles": "error",
        "noStaticElementInteractions": "error",
        "useAltText": "error",
        "useAnchorContent": "error",
        "useAriaActivedescendantWithTabindex": "error",
        "useAriaPropsForRole": "error",
        "useAriaPropsSupportedByRole": "error",
        "useButtonType": "error",
        "useFocusableInteractive": "error",
        "useHeadingContent": "error",
        "useHtmlLang": "error",
        "useIframeTitle": "error",
        "useKeyWithClickEvents": "error",
        "useKeyWithMouseEvents": "error",
        "useMediaCaption": "error",
        "useValidAnchor": "error",
        "useValidAriaProps": "error",
        "useValidAriaRole": {
          "level": "error",
          "options": {
            "allowInvalidRoles": [],
            "ignoreNonDom": false
          }
        },
        "useValidAriaValues": "error",
        "useValidAutocomplete": "error",
        "useValidLang": "error"
      },
      "complexity": {
        "noUselessFragments": "error"
      },
      "correctness": {
        "noChildrenProp": "error",
        "noVoidElementsWithChildren": "error",
        "useJsxKeyInIterable": "error"
      },
      "security": {
        "noBlankTarget": "error",
        "noDangerouslySetInnerHtml": "warn",
        "noDangerouslySetInnerHtmlWithChildren": "error"
      },
      "style": {
        "noImplicitBoolean": "error",
        "noRestrictedGlobals": {
          "level": "error",
          "options": {
            "deniedGlobals": {
              "defaultStatus": "Avoid the deprecated defaultStatus; use window.defaultStatus explicitly if it is truly required.",
              "status": "Use window.status explicitly so it is not confused with a local variable.",
              "scroll": "Use window.scroll explicitly so it is not confused with a local variable.",
              "outerHeight": "Use window.outerHeight explicitly so it is not confused with a local variable.",
              "screenX": "Use window.screenX explicitly so it is not confused with a local variable.",
              "opener": "Use window.opener explicitly so it is not confused with a local variable.",
              "onfocus": "Register focus handlers via window.addEventListener('focus', ...) instead of the implicit onfocus global.",
              "pageYOffset": "Use window.scrollY (or window.pageYOffset) explicitly so it is not confused with a local variable.",
              "addEventListener": "Use window.addEventListener explicitly so it is not confused with a local variable.",
              "defaultstatus": "Avoid the deprecated lowercase defaultstatus; use the standard window.defaultStatus explicitly if it is truly required.",
              "history": "Use window.history explicitly so it is not confused with a local variable.",
              "frames": "Use window.frames explicitly so it is not confused with a local variable.",
              "screenY": "Use window.screenY explicitly so it is not confused with a local variable.",
              "focus": "Use window.focus explicitly so it is not confused with a local variable.",
              "outerWidth": "Use window.outerWidth explicitly so it is not confused with a local variable.",
              "opera": "Avoid the legacy opera global; it is non-standard and unsupported.",
              "external": "Use window.external explicitly so it is not confused with a local variable.",
              "innerHeight": "Use window.innerHeight explicitly so it is not confused with a local variable.",
              "closed": "Use window.closed explicitly so it is not confused with a local variable.",
              "isFinite": "Use Number.isFinite instead https://github.com/airbnb/javascript#standard-library--isfinite",
              "frameElement": "Use window.frameElement explicitly so it is not confused with a local variable.",
              "scrollY": "Use window.scrollY explicitly so it is not confused with a local variable.",
              "self": "Use window.self explicitly so it is not confused with a local variable.",
              "onblur": "Register blur handlers via window.addEventListener('blur', ...) instead of the implicit onblur global.",
              "find": "Use window.find explicitly so it is not confused with a local variable.",
              "parent": "Use window.parent explicitly so it is not confused with a local variable.",
              "top": "Use window.top explicitly so it is not confused with a local variable.",
              "moveBy": "Use window.moveBy explicitly so it is not confused with a local variable.",
              "menubar": "Use window.menubar explicitly so it is not confused with a local variable.",
              "length": "Use window.length explicitly so it is not confused with a local variable.",
              "onerror": "Register error handlers via window.addEventListener('error', ...) instead of the implicit onerror global.",
              "onresize": "Register resize handlers via window.addEventListener('resize', ...) instead of the implicit onresize global.",
              "removeEventListener": "Use window.removeEventListener explicitly so it is not confused with a local variable.",
              "onload": "Register load handlers via window.addEventListener('load', ...) instead of the implicit onload global.",
              "scrollTo": "Use window.scrollTo explicitly so it is not confused with a local variable.",
              "moveTo": "Use window.moveTo explicitly so it is not confused with a local variable.",
              "scrollX": "Use window.scrollX explicitly so it is not confused with a local variable.",
              "name": "Use window.name explicitly so it is not confused with a local variable.",
              "toolbar": "Use window.toolbar explicitly so it is not confused with a local variable.",
              "innerWidth": "Use window.innerWidth explicitly so it is not confused with a local variable.",
              "location": "Use window.location explicitly so it is not confused with a local variable.",
              "locationbar": "Use window.locationbar explicitly so it is not confused with a local variable.",
              "scrollBy": "Use window.scrollBy explicitly so it is not confused with a local variable.",
              "resizeTo": "Use window.resizeTo explicitly so it is not confused with a local variable.",
              "stop": "Use window.stop explicitly so it is not confused with a local variable.",
              "scrollbars": "Use window.scrollbars explicitly so it is not confused with a local variable.",
              "blur": "Use window.blur explicitly so it is not confused with a local variable.",
              "screenTop": "Use window.screenTop explicitly so it is not confused with a local variable.",
              "confirm": "Use window.confirm explicitly so it is not confused with a local variable.",
              "screen": "Use window.screen explicitly so it is not confused with a local variable.",
              "screenLeft": "Use window.screenLeft explicitly so it is not confused with a local variable.",
              "event": "Use the event argument passed to your handler instead of the implicit event global.",
              "onunload": "Register unload handlers via window.addEventListener('unload', ...) instead of the implicit onunload global.",
              "pageXOffset": "Use window.scrollX (or window.pageXOffset) explicitly so it is not confused with a local variable.",
              "resizeBy": "Use window.resizeBy explicitly so it is not confused with a local variable.",
              "statusbar": "Use window.statusbar explicitly so it is not confused with a local variable.",
              "isNaN": "Use Number.isNaN instead https://github.com/airbnb/javascript#standard-library--isnan",
              "close": "Use window.close explicitly so it is not confused with a local variable.",
              "open": "Use window.open explicitly so it is not confused with a local variable.",
              "print": "Use window.print explicitly so it is not confused with a local variable."
            }
          }
        },
        "useConsistentCurlyBraces": "error",
        "useFragmentSyntax": "error"
      },
      "suspicious": {
        "noAlert": "warn",
        "noArrayIndexKey": "error",
        "noCommentText": "error",
        "noDuplicateJsxProps": "error"
      }
    }
  }
}
