{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // page.evaluate などブラウザコンテキストのコードで DOM 型を参照するため DOM を含める
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "types": ["@playwright/test", "node", "@cloudflare/workers-types"]
  },
  "include": ["src", "playwright.config.ts"]
}
